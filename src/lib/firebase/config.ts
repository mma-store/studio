/**
 * @fileOverview Redundant config file cleared to prevent conflicts.
 * The canonical config is located in src/firebase/config.ts
 */
import { firebaseConfig } from '@/firebase/config';
export { firebaseConfig };
